name: Generate Snake Animation

on:
  schedule:
    # Runs every 12 hours to keep the snake fresh
    - cron: "0 */12 * * *"
  # Lets you trigger it manually from the Actions tab
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate snake game from contributions
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=58A6FF&color_dots=161B22,1F6FEB,58A6FF,79C0FF,D2F4FF
            dist/github-contribution-grid-snake.svg?color_snake=1F6FEB&color_dots=EBEDF0,79C0FF,58A6FF,1F6FEB,0A3069

      - name: Push snake to output branch
        uses: crazy-max/ghaction-github-pages@v4.0.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
